PRIORITIES in time order:

- tensor ops:
 - DONE CPU add, sub, mul
 - DONE CPU matmul
 - DONE GPU add, sub, mul
 - DONE GPU matmul

- DONE benchmarks:
 - CPU
 - GPU
 - pytorch on GPU

- DONE begin optimization

- keep a readme log and maybe git commits for optimization

OTHER TODOS:

- DONE Python initialize from list
- DONE Python __getitem__ and __setitem__
- DONE Python enable access to dim, shape, offset, strides, device
- DONE Python __getitem__ and __setitem__, allow optional integer argument
- DONE C++, change ugly pointers to array indexing

- CUDA synchronize command, make it available to python
- CUDA, improve error checking
- Python pretty print
- Python assignment 'x2 = x1' should create new object with same data
- Python implement __eq__
- Python: tests:
  - matmul and other ops, same result on CPU and GPU
- Disable mixed CPU/GPU compute
- GPU random number generation

- Views and reshaping:
	- Allow creation of views by numpy-style indexing
	- Allow general reshaping
- Additional operations:
	- range
	- DONE random
	- sum along axis
	- scalar mul / div

NOTES:

cuda-memcheck and valgrind
bank conflicts and memory coalescing (4/32 bytes per sector)

