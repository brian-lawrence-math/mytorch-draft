[project]
name = "mytorch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cuda-python>=13.1.1",
    "pybind11>=3.0.2",
]
